<% if @candidate.errors.any? %>
    <% @candidate.errors.full_messages.each do |message| %>
      <%= message %><br/>
    <% end %>
<% end %>

<h1>Perfil</h1>

<%= form_with model: @candidate, local: true do |f| %>
  <div class="field">
    <%= f.label :name, 'Nome' %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :birthday, 'Data de Nascimento' %><br />
    <%= f.date_field :birthday %>
  </div>
  
  <div class="field">
    <%= f.label :scholarity, 'Escolaridade' %><br />
    <%= f.text_area(:scholarity, size: "24x3") %>
  </div>

  <div class="field">
    <%= f.label :work_experience, 'Experiência Profissional' %><br />
    <%= f.text_area(:work_experience, size: "24x3") %>
  </div>

  <div class="field">
    <%= f.label :job_interest, 'Vagas de Interesse' %><br />
    <%= f.text_field :job_interest %>
  </div>

  <div class="field">
    <%= f.label :photo, 'Foto de Perfil' %><br />
    <%= f.file_field :photo %>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit "Completar Perfil" %>
  </div>
<% end %>