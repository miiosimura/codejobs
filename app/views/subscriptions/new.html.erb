<%= form_with model: @subscription, local: true do |s| %>
  <%= s.label :about_candidate, 'Conte um pouco sobre você :)' %>
  <%= s.text_area(:about_candidate, size: "24x6") %>
  <%= s.submit 'Salvar inscrição' %>
<% end %>